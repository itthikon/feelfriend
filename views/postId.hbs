<!DOCTYPE html>
<html lang="en">
{{> baseHead customTitle=customTitle}}
<body>
    {{> siteHeader }}
    <div>
        {{#if onePost }}
            <div style="background-color: rgb(235, 131, 131);">
                <h2>{{ onePost.title }}</h2>
                <p>
                    <span>{{ onePost.from }}</span>
                    <span>{{ onePost.createdAtText }}</span>
                </p>
            </div>
        {{else}} 
            <p>ไม่พบโพสต์ที่คุณต้องการดู</p>
        {{/if}}
    </div>
    {{> siteFooter }}
</body>
</html>